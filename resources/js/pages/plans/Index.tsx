import PricingPage from '@/components/pricing/default';
import Layout from '@/layouts/layout';

function plans() {
    return (
        <Layout>
            <PricingPage />
        </Layout>
    );
}

export default plans;
